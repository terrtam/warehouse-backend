spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/warehouse
    username: postgres
    password: ILikeOJ123!

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

jwt:
  # Minimum 32 ASCII chars for HS256. Replace in production.
  secret: "dev-secret-change-me-please-32chars"
  expiration-ms: 3600000
